# OpenRouter API (required)
OPENROUTER_API_KEY=your_openrouter_api_key

# Default model for OpenRouter (optional, has a sane default)
# Example: anthropic/claude-3.5-sonnet
DEFAULT_LLM_MODEL=anthropic/claude-3.5-sonnet

# Optional metadata for OpenRouter requests
# OPENROUTER_SITE_URL=https://your-site.example.com
# OPENROUTER_SITE_NAME=Your App Name

# Optional: override the OpenRouter base URL
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Google service account JSON (required for Sheets features)
# Recommended local setup:
#   1. Copy python_backend/service-account.example.json to
#      python_backend/service-account.json and fill in your real values.
#   2. Leave GOOGLE_SERVICE_ACCOUNT_JSON unset.
#   3. Optionally set GOOGLE_SERVICE_ACCOUNT_FILE to override the path.
#
# On hosted platforms (e.g. Railway) where mounting files is inconvenient,
# you can instead paste the full JSON into an env var:
#
#   GOOGLE_SERVICE_ACCOUNT_JSON='{"type": "...", "project_id": "...", ...}'
#
# If GOOGLE_SERVICE_ACCOUNT_JSON is not set, the backend will look for:
#   - GOOGLE_SERVICE_ACCOUNT_FILE (absolute path)
#   - python_backend/service-account.json
#   - legacy paths in the repo root
#
# GOOGLE_SERVICE_ACCOUNT_FILE=/absolute/path/to/your-service-account.json
